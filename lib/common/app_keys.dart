class AppKeys {
  // 인스턴스화 방지
  AppKeys._();

  // SecureStorage Keys
  static const String accessToken = 'accessToken';
  static const String userId = 'userId';
  static const String userName = 'userName';

// 필요한 경우 Hero Tag나 라우트 이름도 여기서 관리 가능
}